# DEPRECATED: This file is kept for backward compatibility only.
#
# Please use pyproject.toml for dependency management (PEP 621 standard):
#   pip install -e .           # Install with core dependencies
#   pip install -e ".[dev]"    # Install with dev dependencies
#   pip install -e ".[all]"    # Install with all optional dependencies
#
# This requirements.txt will be removed in a future version.
# See pyproject.toml for the canonical dependency list.

# Core dependencies
pyyaml>=6.0  # ConfigLoader: YAML configuration support
